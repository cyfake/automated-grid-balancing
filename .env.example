# Grid Balance Agent — Environment Variables

# Agentfield server URL (default: http://localhost:8080)
AGENTFIELD_SERVER=http://localhost:8080

# EIA Open Data API key (optional — falls back to DEMO_KEY if not set)
# Get a free key at: https://www.eia.gov/opendata/register.php
# DEMO_KEY works but has stricter rate limits (30 req/hour).
# EIA_API_KEY=your-key-here

# LLM Summary (disabled by default to avoid rate limits)
# Set to "true" to enable Gemini-powered narrative summaries
ENABLE_LLM_SUMMARY=false

# Gemini API key (only needed if ENABLE_LLM_SUMMARY=true)
# GEMINI_API_KEY=your-key-here
